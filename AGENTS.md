# Codex Instructions

If related repositories are checked out as siblings, treat that only as local
context. Do not write documentation that assumes repositories live at relative
filesystem paths to each other; use repository names or GitHub URLs for
cross-repository references.
